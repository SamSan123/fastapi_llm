[project]
name = "fastapi-project"
version = "0.1.0"
description = "dependencies"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["fastapi", "pydantic", "google-genai", "uvicorn", "jinja2", "python-multipart"]
